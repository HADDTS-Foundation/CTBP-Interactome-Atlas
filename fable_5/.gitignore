# Pipeline build intermediates — regenerated by `python3 data/run_all.py`,
# not shipped to the browser. app-data.js IS tracked (the app loads it).
data/cache/
data/_work.json
data/_work.json.bak-*
app-data.js.bak-*
data/__pycache__/
# BioGRID: ignore the large raw archive(s); the small CtBP extract .tsv IS committed
data/BioGRID/*.zip
data/BioGRID/*.tab3.txt
data/BioGRID/*.part
*.log
*.tmp
.DS_Store
